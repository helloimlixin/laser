# Data configuration
dataset: cifar10
data_dir: ${hydra:runtime.cwd}/data/cifar10
batch_size: 64
num_workers: 4
image_size: 32
mean: [0.4914, 0.4822, 0.4465]  # CIFAR-10 channel means
std: [0.2470, 0.2435, 0.2616]   # CIFAR-10 channel stds
valid_ratio: 0.1  # 10% of training set for validation 